docker stop vite-dev
docker run --rm -it --name vite-dev -p 5173:5173 \
  -v "$HOME/remote_mount/Private/Server1/Documents/HomePage/app":/app \
  -v vite_node_modules:/app/node_modules \
  -w /app node:20-alpine sh -lc \
  "npm run dev -- --host 0.0.0.0 --port 5173"

  three js
  docker run --rm -it \
  -v "$HOME/remote_mount/Private/Server1/Documents/HomePage":/app \
  -v vite_node_modules:/app/node_modules \
  -w /app \
  node:20-alpine sh -lc \
  "npm install three && npm install -D @types/three"